<% unless current_user?(@user) %>
  <div id="follow_form">
  <% if current_user.following?(@user) %>
    <%= render t ("follow.unfollow") %>
  <% else %>
    <%= render t ("follow.follow") %>
  <% end %>
  </div>
<% end %>
